version: 2

before:
  hooks:
    - npm run build --prefix ui

builds:
  - binary: "{{ .ProjectName }}"
    ldflags:
      - -s -w
      - -X github.com/prasenjit-net/orchestra/internal/version.Version={{ .Version }}
      - -X github.com/prasenjit-net/orchestra/internal/version.Commit={{ .Commit }}
      - -X github.com/prasenjit-net/orchestra/internal/version.BuildDate={{ .Date }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      - goos: windows
        goarch: arm64

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
